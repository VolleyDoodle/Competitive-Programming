#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("txt.in", "r", stdin);
    freopen("txt.out", "w", stdout);
    int n;
    cin >> n;
    vector <vector <int>> graph(n + 1);
    
    for (int i = 1; i <= n; i++){
        int tt;
        cin >> tt;
        graph[i].push_back(tt);
    }
    for (int i = 1; i <= n; i++){
        
    }
}