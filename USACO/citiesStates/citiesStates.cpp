#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("citystate.in", "r", stdin);
    freopen("citystate.out", "w", stdout);
    unordered_map<vector <int>, int>> areasCodes;
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int n; cin >> n;
    for (int i = 0; i < n; i++){
        char t, tt; cin >> t >> tt;
        string ttt; cin >> ttt;
        if (areasCodes.count({}))
    }
}