#include <iostream>
#include <vector>
#include <map>
#include <string>
using namespace std;
int main()
{
	
	long long ans = 0;
	string s;
	cin >> s;
	int n = s.length();
	vector <int> pfs(n);
	pfs.push_back(0);
	for (int i = n - 1; i >= 0; i--) {
		
		
	}

}
