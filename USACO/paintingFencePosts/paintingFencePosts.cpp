#include <bits/stdc++.h>
#define ll long long
#define s second
#define f first
using namespace std;

void setIO(string prob) {
    freopen((prob + ".in").c_str(), "r", stdin);
    freopen((prob + ".out").c_str(), "w", stdout);
}
//frickin annoying ahh problem
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    setIO("txt");
    int n, p;
    cin >> n >> p;
    for (int i = 0; i < p; i++){

    }

    for (int i = 0; i < n; i++){

    }
}